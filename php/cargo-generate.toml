[template]
cargo_generate_version = ">=0.10.0"
ignore = [
    "actix/**/*",
    "tauri/**/*",
    "vanilla/**/*",
    "nodejs/**/*",
]

[placeholders]
api_port = { type = "string", prompt = "Enter the API port", default = "8000" }
